# Vercel
.vercel

# Env files
.env*.local

# macOS
.DS_Store

# Project management & strategy artifacts (live alongside code but are not part of the app)
*.docx
Reshape and Funder-Readiness Memo.md
Local Git and Vercel Workflow.md

# Build artifacts that don't belong in source control
api/*.zip

# Email render previews (verification artifacts, not deployed)
email-previews/

# Local test harness artifacts (mock emails + screenshots regenerate on every run)
test/outbox/
test/screenshots/
